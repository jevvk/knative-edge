---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: edges.operator.edge.jevv.dev
spec:
  group: operator.edge.jevv.dev
  names:
    kind: Edge
    listKind: EdgeList
    plural: edges
    singular: edge
  scope: Namespace
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.zone
      name: Zone
      type: string
    - jsonPath: .status.region
      name: Region
      type: string
    - jsonPath: .status.environments
      name: Environments
      priority: 1
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Edge is the Schema for the Edges API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EdgeSpec defines the desired state of Edge
            properties:
              clusterName:
                description: The name of the Edge Cluster. This is used for retrieving
                  EdgeCluster from the remote cluster.
                type: string
              overrideProxyImage:
                description: Override the proxy image for forwarding edge requests
                  to the cloud.
                type: string
              secretRef:
                description: The secret containing the kubeconfig to the remote cluster.
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
            type: object
          status:
            description: EdgeStatus defines the observed state of Edge
            properties:
              environments:
                description: The list of environments which are replicated to the
                  edge cluster.
                items:
                  type: string
                type: array
              observedGenerationDeployment:
                description: The observed generation of the Deployment
                format: int64
                type: integer
              observedGenerationEdge:
                description: The observed generation of the Edge
                format: int64
                type: integer
              observedGenerationEdgeCluster:
                description: The observed generation of the EdgeCluster
                format: int64
                type: integer
              region:
                description: The region where the edge cluster is located.
                type: string
              zone:
                description: The zone of the edge cluster.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
